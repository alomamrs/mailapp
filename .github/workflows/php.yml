name: PHP Lint

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.3'

    - name: Check PHP syntax
      run: find . -name "*.php" -type f -exec php -l {} \; > /tmp/phplint.log 2>&1; if grep -i "Parse error" /tmp/phplint.log; then false; else true; fi
